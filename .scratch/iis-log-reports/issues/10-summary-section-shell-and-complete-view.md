# 10 — Summary section (shared shell, sidebar, and Complete View)

**What to build:** The first real pages of the Dashboard — the shared HTML shell/sidebar/footer module every later page reuses, and the Summary section itself: a per-Root total-requests line chart and a sortable Complete View table (hits, total time taken, average time taken per Root), each with All and Last 7 Days variants. Proves the whole pipeline (query → embed → render) end-to-end on the simplest section before any other section is built.

**Blocked by:** 08, 09.

**Status:** done

- [x] Shared page shell (header/sidebar-mount-point/footer) renders correctly, including the "Created on `<timestamp>` / Generated by `<tool>`" footer baked in at generation time.
- [x] Sidebar lists Summary, Portal, ArcGIS Server, and Leaderboard as top-level entries (later tickets fill in Portal/ArcGIS Server/Leaderboard's own pages), with no separate "Home" entry.
- [x] The site's `index.html` and the sidebar brand both resolve to Summary → Successful Requests → All.
- [x] Summary's total-requests-per-Root evolution renders as a single multi-series line chart (all Included Roots overlaid on one chart), All and Last 7 Days as separate pages — no successful/failed split at this level.
- [x] Summary's Complete View table (hits, total time taken, average time taken, one row per Root) is sortable by column header, sourced from the existing by-root aggregate directly — no new SQL view needed for this section.
- [x] Verified against the harvested fixture database (ticket 09): totals in the Complete View match an independently computed oracle from the fixture data.

## Comments

Implemented in `4e1bb3e`: new query classes `ByRootDailyTotalsQuery`/`ByRootRangeTotalsQuery` (`src/IisLogParserArcGIS.Data/Queries/`) - the first `AggregateQueryBase` subclasses in the codebase - select directly against `aggregated_by_root` (no view, per ticket 03) for a date range's per-day-per-Root hits and per-Root totals respectively. `src/IisLogParserArcGIS.Reports/Rendering/` holds the shared shell: `PageShellRenderer` wraps every page's body in a header/sidebar/footer, computing every link (brand, sidebar, stylesheet) relative to that page's own output depth so the Dashboard also works opened straight from disk; `DashboardSidebar` builds the fixed Summary → Successful Requests → {All, Last 7 Days} / Complete View tree plus placeholder Portal/ArcGIS Server/Leaderboard entries; `GoogleChartsRenderer` renders the inline `LineChart`/`Table` bootstrap scripts (Google Charts via CDN, per ticket 01) - the line chart declares its columns explicitly (`ChartColumn`/`LineChartData`) rather than relying on `arrayToDataTable`'s first-row type inference, since a low-traffic Root can have a `null` (no-data) cell on the very first plotted date, which breaks inference. `SummarySectionBuilder` (`src/IisLogParserArcGIS.Reports/Sections/`) ties queries to rendering and writes `index.html` plus the three Summary pages; `RegenerationRun.Run` now calls it after (re)creating the output directory. `Program.cs`'s Dashboard-regeneration catch clause now also catches `SqliteException`, since this is the first ticket where that call path runs real SQL. Test coverage: `ByRootDailyTotalsQueryTests`/`ByRootRangeTotalsQueryTests` (range filtering, root filtering, summing, empty/null-argument cases) in `Data.Tests`; `SummarySectionTests` in `Reports.Tests` drives `RegenerationRun.Run` end-to-end against the ticket-09 harvested fixture (pinned to the corpus's last real date, 2026-09-10) and asserts every expected file is written, `index.html` matches the Summary → Successful Requests → All page's own content, the Complete View's per-Root hit totals match a hand-written independent SQL oracle, and every real Included Root appears as its own chart series. Spot-checked the real compiled exe against the real corpus: confirmed the generated HTML's relative links, sidebar active-state, and footer timestamp are all correct at every page depth, and confirmed a Root with a `null` first-date cell (`galatea`) still gets an explicit `addColumn('number', ...)` rather than triggering Google Charts' type-inference failure. Full suite green: 287 tests passing across all six test projects.
